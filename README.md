# FlushOS
A Operating System Still In Construction

Execute ./Builds/Scripts/init.sh to download and build all nessecery packages and programms, like download the nasm assember , qemu virtual machine
and build the cross platform gcc compiler localy in your system . To run in virtual machine the code then after the init.sh run the build.sh 